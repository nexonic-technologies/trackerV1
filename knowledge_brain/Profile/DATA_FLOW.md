# Data Flow: Profile

## API Payloads
Extracted from React Components targeting the generic API endpoint.

- **index.jsx** -> `GET /populate/read/employees/${user.id}?populateFields=${encodeURIComponent(JSON.stringify(pop))}`
- **index.jsx** -> `PUT /populate/update/employees/${user.id}`
